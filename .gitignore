# Dependencies
node_modules/
.venv/

# Python bytecode
__pycache__/
*.py[cod]

# Build output
dist/
.next/
next-env.d.ts

# Playwright artifacts
test-results/
playwright-report/
.playwright/

# Coverage
coverage/
.coverage
htmlcov/
.pytest_cache/

# OS
.DS_Store

# Vendored upstream repos — clones excluded, pins/readme tracked (finalized in Шаг 0.9)
vendor/*
!vendor/PINS.lock
!vendor/README.md

# Design exploration scratch (P1.4 checkpoint B) — kept locally, not in VCS
.playwright-mcp/
design-explorations/
p1.4-landing-dark.png
fh-1440-hero.png

# Local test media for the P2 clip engine — large binaries, kept out of VCS
samples/
