{
  "name": "@fliphouse/worker-node",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@fliphouse/shared": "workspace:*",
    "bullmq": "^5.78.1",
    "ioredis": "^5.11.1"
  }
}
