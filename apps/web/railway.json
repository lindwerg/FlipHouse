{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm run build:next"
  },
  "deploy": {
    "startCommand": "next start -H ::",
    "healthcheckPath": "/api/health",
    "preDeployCommand": "npm run db:migrate",
    "numReplicas": 2,
    "restartPolicyType": "ON_FAILURE"
  }
}
